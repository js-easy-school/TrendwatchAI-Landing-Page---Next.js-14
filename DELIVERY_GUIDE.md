# 🎁 Инструкция по передаче проекта клиенту

## Способ 1: Через GitHub (рекомендуется)

### Шаг 1: Создай приватный репозиторий

1. Зайди на [github.com](https://github.com) → New repository
2. Название: `trendwatch-landing` (или как хочет клиент)
3. **Private** — приватный репозиторий
4. Не добавляй README (он уже есть)

### Шаг 2: Загрузи проект

```bash
cd "путь/к/trendwatch-nextjs"
git init
git add .
git commit -m "Initial commit - TrendwatchAI Landing"
git branch -M main
git remote add origin https://github.com/ТВОЙ_ЛОГИН/trendwatch-landing.git
git push -u origin main
```

### Шаг 3: Передай доступ клиенту

**Вариант A: Передать репозиторий**

1. Settings → General → Danger Zone → Transfer ownership
2. Введи GitHub username клиента
3. Клиент получит полный контроль

**Вариант B: Добавить как collaborator**

1. Settings → Collaborators → Add people
2. Введи email или username клиента
3. Выбери роль: Admin (полный доступ)

**Вариант C: Скачать ZIP**

1. На странице репозитория: Code → Download ZIP
2. Отправь ZIP клиенту

---

## Способ 2: Через файлообменник

### Подготовка архива

1. Удали папку `node_modules` (она тяжёлая и не нужна)
2. Удали папку `.next` если есть
3. Заархивируй папку `trendwatch-nextjs` в ZIP

### Что должно быть в архиве:

```
trendwatch-nextjs/
├── src/
├── public/
├── .github/
├── package.json
├── next.config.js
├── tsconfig.json
├── README.md
├── LICENSE
└── .gitignore
```

### Куда загрузить:

- Google Drive
- Яндекс.Диск
- Dropbox
- WeTransfer

---

## 📋 Что сказать клиенту

### Минимальные требования:

- Node.js 18 или выше
- 5 минут на установку

### Быстрый старт:

```bash
npm install
npm run dev
```

### Для продакшена:

```bash
npm run build
```

Папка `out` — готовый сайт для любого хостинга.

### Рекомендуемый хостинг:

- **Vercel** — бесплатно, автодеплой из GitHub
- **Netlify** — бесплатно
- **GitHub Pages** — бесплатно

---

## ✅ Чеклист перед передачей

- [ ] Удалена папка node_modules
- [ ] Удалена папка .next
- [ ] README.md обновлён
- [ ] LICENSE файл есть
- [ ] Все тексты на нужном языке
- [ ] Контактные ссылки заменены на клиентские
- [ ] Google Calendar URL заменён
- [ ] Проверен build (npm run build)
